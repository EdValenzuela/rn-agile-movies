import { GET_MOVIES } from "../../types";


const AgileMoviesReducer = (state, action) => {
    switch (key) {
        case GET_MOVIES:
            return{

            }
    
        default:
            return state;
    }
}

export default AgileMoviesReducer